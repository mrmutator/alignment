#PBS -S /bin/bash
#PBS -lnodes=1:cores16
#PBS -lwalltime=24:00:00
cd "$TMPDIR"
mkdir data
cp %(it_dir)s/%(job_name)s.*.prms data/
cp %(it_dir)s/%(job_name)s.*.counts data/
# call the programs
python %(script_dir)s/word_alignment/models/hmm/update_parameters.py -dir data/ -num_workers 4
cp data/*.u %(it_dir)s/
cp log_likelihood %(it_dir)s/
wait          # wait until programs are finished
